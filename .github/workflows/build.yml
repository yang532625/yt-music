name: Build APKs

# Disabled Metrolist nightly builder. Releases are published by publish/release.yml.
on:
  workflow_dispatch:

jobs:
  skipped:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use the Publish YT Music workflow to ship updates."
