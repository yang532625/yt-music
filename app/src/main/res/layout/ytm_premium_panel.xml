<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/ytm_premium_panel"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#030303"
    android:fillViewport="true"
    android:visibility="gone">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="24dp"
        android:paddingTop="16dp"
        android:paddingEnd="24dp"
        android:paddingBottom="32dp">

        <ImageView
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:contentDescription="@string/app_name"
            android:src="@drawable/ytm_logo" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:text="@string/premium_title"
            android:textColor="#FFFFFF"
            android:textSize="28sp"
            android:textStyle="bold" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:background="@drawable/ytm_premium_badge"
            android:paddingStart="10dp"
            android:paddingTop="4dp"
            android:paddingEnd="10dp"
            android:paddingBottom="4dp"
            android:text="@string/premium_badge"
            android:textColor="#030303"
            android:textSize="12sp"
            android:textStyle="bold" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:text="@string/premium_subtitle"
            android:textColor="#AAAAAA"
            android:textSize="15sp" />

        <include layout="@layout/ytm_premium_row" android:id="@+id/ytm_premium_row_ads" />
        <include layout="@layout/ytm_premium_row" android:id="@+id/ytm_premium_row_bg" />
        <include layout="@layout/ytm_premium_row" android:id="@+id/ytm_premium_row_quality" />
        <include layout="@layout/ytm_premium_row" android:id="@+id/ytm_premium_row_skips" />
        <include layout="@layout/ytm_premium_row" android:id="@+id/ytm_premium_row_video" />
        <include layout="@layout/ytm_premium_row" android:id="@+id/ytm_premium_row_yt" />
        <include layout="@layout/ytm_premium_row" android:id="@+id/ytm_premium_row_offline" />
    </LinearLayout>
</ScrollView>
